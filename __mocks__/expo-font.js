// __mocks__/expo-font.js

// Define the mock module
const ExpoFont = {
  // Mocked functions or data
  // For example:
  loadAsync: jest.fn().mockResolvedValue({
    /* mocked response */
  }),
};

export default ExpoFont;
