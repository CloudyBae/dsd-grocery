version: "3.9"

services:
  zbackend:
    build:
      context: .
      args:
        - DEV=true
    ports:
      - "8000:8000"
    volumes:
      - ./zbackend:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"
